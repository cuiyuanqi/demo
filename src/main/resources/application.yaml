server:
    port: 8080
mybatis:
    mapper-locations: classpath:/mapper/*.xml
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/demo_sever?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
        username: root
        password: root
    rabbitmq:
        host: 127.0.0.1
        port: 5672
        virtualHost: /
        username: guest
        password: guest
